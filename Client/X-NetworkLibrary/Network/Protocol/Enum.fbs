
// +------------
// Enum.fbs
// ------------+

namespace FBProtocol;

enum OBJECTTYPE : byte {
  NONE,
  PLAYER,
  MONSTER,
  STRUCTURE
}

enum MOVESTATE : byte{
	MOVE_START,
	MOVE_PROGRESS,
	MOVE_END
}